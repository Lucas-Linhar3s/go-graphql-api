type Person {
    id: ID!
    name: String!
    age: Int!
    isMale: Boolean!
}

type CreatePersonInput {
    name: String!
    age: Int!
    isMale: Boolean!
}

extend type Query {
    persons: [Person!]!
}

# extend type Mutation {
    # createPerson(input: CreatePersonInput!): Person!
# }